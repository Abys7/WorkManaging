<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkManaging">
        <activity
            android:name=".view.activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.MainActivity" />
        <activity android:name=".view.activity.ClientsActivity" />
        <activity android:name=".view.activity.ProjectsActivity" />
        <activity android:name=".view.activity.SettingsActivity" />
        <activity android:name=".view.activity.RegisterActivity" />
        <activity android:name=".view.activity.NewClientActivity" />
        <activity android:name=".view.activity.ClientDetailActivity" />
        <activity android:name=".view.activity.NewProjectActivity" />
        <activity android:name=".view.activity.ProjectDetailActivity" />
        <activity android:name=".view.activity.AccountActivity" />
    </application>

</manifest>